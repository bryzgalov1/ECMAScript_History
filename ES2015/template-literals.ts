
const userName = "Alice";
const age = 25;

// Интерполяция строк
const message = `Привет, меня зовут ${userName} и мне ${age} лет.`;
console.log(message);

// Многострочные строки
const multiline = `
  Это первая строка
  Это вторая строка
`;
console.log(multiline);
