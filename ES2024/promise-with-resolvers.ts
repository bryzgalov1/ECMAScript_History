
// Создает Promise и возвращает resolve/reject методы рядом
/*
const { promise, resolve, reject } = Promise.withResolvers();

promise.then((v) => console.log("Resolved:", v));

setTimeout(() => {
  resolve("Done!");
}, 1000);
*/
